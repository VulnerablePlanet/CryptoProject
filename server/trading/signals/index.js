/**
 * ============================================================================
 * SIGNALS INDEX
 * ============================================================================
 * Main entry point for signal generation.
 */

const { SIGNAL_TYPES, generateSignal, explainSignal } = require('./engine')

module.exports = {
  SIGNAL_TYPES,
  generateSignal,
  explainSignal
}
